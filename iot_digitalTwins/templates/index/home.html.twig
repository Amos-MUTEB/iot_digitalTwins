{% extends 'base.html.twig' %}

{% block title %}Appli digital twins!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <h1 class="title">Digital Twins Coronavirus ✅</h1>
<divc id="main_container">
    <div id="result">
        <h3 class="alignText">Mes données recupérées</h3>
        <div class="column1">
        <div class="mb-3">
            Connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
        <p>Mon ID : {{ app.user.id }}</p>
        </div>
    </div>

    <div id="result">
        <h3>Mon relevé: </h3>
        <div class="column2">
            <label for="inputEmail" class="sr-only">pseudo</label>
            <p>Température actuelle : {{ temperature }} </p>
             <a href="{{ path('publish', {'temperature': temperature, 'id': app.user.id}) }}"> <button class="btn btn-lg btn-primary btn-block" type="submit">PUBLISH</button> </a>
        </div>
    </div>

    <div id="result">
        <h3>J'ai croisé quelqu'un: </h3>
        <div class="column2">
            <label for="inputPassword" class="sr-only">ID de la personne</label>
            <input type="password" id="inputPassword" class="form-control" placeholder="ID de la personne" required>

            <button class="btn btn-lg btn-primary btn-block" type="submit">SUBSCRIBE</button>
        </div>
    </div>
</div>
{% endblock %}
